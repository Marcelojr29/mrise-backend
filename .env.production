# App
NODE_ENV=production
PORT=10000
APP_NAME=MRISE TECH API

# MongoDB (Render vai criar automaticamente se usar o config acima)
# OU use seu MongoDB Atlas existente:
MONGODB_URI=mongodb+srv://marcelo:Mb93220408@mbjr.wsgyx8n.mongodb.net/mrisetech?retryWrites=true&w=majority

# JWT (GERAR UMA CHAVE SEGURA!)
JWT_SECRET=SEU_JWT_SECRET_SUPER_SEGURO_AQUI_MUDAR_NO_RENDER
JWT_EXPIRATION=8h

# CORS (coloque seu frontend da Vercel)
CORS_ORIGIN=https://mrise-frontend.vercel.app